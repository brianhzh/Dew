# Dew

Person A (`nathan-ui`) is the React client. Person B is Snowflake + API.

**Start here if you are B:** [`shared/FOR-B.md`](shared/FOR-B.md)

**Start the phone UI:**

```bash
cd client
npm install
npm run dev
```

`VITE_OFFLINE=true` (default). Flip to `false` when B’s API is up.
